#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main()
{
    int n;
    vector<int> vec(n, 0);
    map<int, int> m;
    auto it = m.find(1);
    find(vec.begin(), vec.end(), 1);
}